var conf = {    clazz: {        type: 'method',        'method': 'baidu.async.when'    },        demoType: [{key: 'default', val: 'baidu.async.when'}],    'default': {        pageConf: {            html:'<div class="Conmain">'+						'<div id="resultArea" style="">#resultArea</div>'+						'<div class="explain">'+						'当async执行完成时候根据结果执行不同的函数方法。（此示例仅接受数值类型的值）'+						'<br />JS代码'+						'<code>'+												'<p>baidu.async.when(T.async.get("images/ajaxRequest.php?x=5&y=2"),</p>'+							'<p>  function(obj){T.g("resultArea").innerHTML = obj.responseText;},</p>'+							'<p>  function(obj){T.g("resultArea").innerHTML = "请求失败";}</p>'+						'<p>);</p>'+																		'</code>'+						'<br />PHP代码'+						'<code>echo "来自服务器端的响应x:".($_REQUEST["x"]-0).",y:".($_REQUEST["y"]-0);</code>'+						'</div>'+					'</div>'        },        btn1: {            type: 'button',            defaultValue: '正确地址请求',            isMain: true,            event: {            	eventName: 'onclick',            	handler: function(){					baidu.async.when(T.async.get('images/ajaxRequest.php?x=5&y=2'),						function(obj){							T.g('resultArea').innerHTML = obj.responseText;						},						function(obj){							T.g('resultArea').innerHTML = 'when Get Faild!';						} 					);					            	}            }        }    },    groups: {        'default': [['btn1']]    }};  